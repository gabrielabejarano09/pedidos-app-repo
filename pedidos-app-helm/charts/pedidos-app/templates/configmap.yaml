apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-backend-config
data:
  DB_HOST: "{{ .Release.Name }}-db"
  DB_PORT: "5432"
  DB_NAME: "{{ .Values.db.auth.database }}"
  DB_USER: "{{ .Values.db.auth.username }}"