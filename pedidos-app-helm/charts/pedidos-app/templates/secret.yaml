apiVersion: v1
kind: Secret
metadata:
  name:  {{ .Release.Name }}-db-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData: 
  password: {{ .Values.db.auth.password | required "db.auth.password is required" | quote }}
